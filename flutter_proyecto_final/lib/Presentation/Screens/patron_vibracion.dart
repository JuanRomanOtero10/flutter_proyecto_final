import 'package:flutter/material.dart';

class PatronVibracion extends StatelessWidget {
  const PatronVibracion({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Patrón de Vibración'),
      ),

    );
  }
}