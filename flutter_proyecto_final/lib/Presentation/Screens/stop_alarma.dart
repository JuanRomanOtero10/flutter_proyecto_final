import 'package:flutter/material.dart';

class StopAlarma extends StatelessWidget {
  const StopAlarma({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}