import 'package:flutter/material.dart';

class PatronLuz extends StatelessWidget {
  const PatronLuz({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Patrón de Luz'),
      ),
    );
  }
}